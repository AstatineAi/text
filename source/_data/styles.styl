.post-body p {
  text-indent: 2em;
}

// Give article text a little more presence without changing the interface scale.
.post-body {
  font-size: 1.0625em;

  @media (min-width: 1200px) {
    font-size: 1.2em;
  }
}
